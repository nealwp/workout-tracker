version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build:web --workspace=@irondog/web
  artifacts:
    baseDirectory: packages/web/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*